# Example environment variables for Supabase setup

# For Supabase client (keep your current values)
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_anon_key

# For Prisma migrations - you need BOTH:
# 1. Direct connection (for migrations)
DATABASE_URL="postgresql://postgres.[your-project-ref]:[your-password]@aws-0-us-east-1.compute.amazonaws.com:5432/postgres"

# 2. Pooler connection (for application use - optional)
DATABASE_URL_POOLER="postgresql://postgres.[your-project-ref]:[your-password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Other env vars
NODE_ENV=development
PORT=5000